# Build output
[Bb]in/
[Oo]bj/

# IDE and editor state
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# Local tooling and generated files
.config/dotnet-tools.json
TestResults/
*.tmp
*.log

# Large source assets supplied outside Phase 1
/erika/

# MGCB build exhaust for the out-of-tree canonical Erika source (see README):
# the model .xnb lands next to Content.mgcb and texture .xnbs land under a
# mixamo-mini tree next to the project. Both regenerate via `dotnet build`.
src/ErikasLab.Platform.MonoGame/Content/erika/
**/mixamo-mini/

# Custom content-pipeline build product loaded by MGCB (see README).
# Regenerates via `dotnet build` through the platform project reference.
BuildTools/
